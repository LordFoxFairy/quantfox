# 交接文档 · quantfox skills 打磨（2026-07-10）

> 2026-07-10 晚：P1（一致性+全局统一+mandate+对账留痕）已完成并合入 main，本文档剩余工作已被 docs/superpowers/plans/2026-07-10-quantfox-p1-consistency-mandate.md 覆盖；「未提交」一节已过时（package.json 已删、uv.lock 已同步）。

> 给下一个 code agent。读这一份 + `docs/skill-improvements.md` 即可接手，不必回溯整段会话。
> 分支：`feat/p1-quant-assistant`。**未 push**。

## 1. 背景一句话
一整轮实战（记账博时黄金 002611 + 全市场选基）暴露了记账/深筛/前瞻/诚实性的缺口，已迭代出 v13 框架（保本优先、看中位不看均值、估值条件化）。多数缺口已实现，剩少量收尾。

## 2. 当前状态（已完成 + 已验证）
| 项 | 状态 | 验证 |
|---|---|---|
| A1/A2/A3 按金额多笔记账（`lots` 表 / `watch buy --amount` / `watch position`）| ✅ | `test_lots.py` 3 绿 |
| B1 估值闸门（fund-screener 铁律：深筛分≠能买，>0.85 剔除）| ✅ | SKILL 文本 |
| B2 大盘 regime 前置（screen-report 头含 market-valuation）| ✅ | 命令存在 |
| B4 `quantfox screen-report`（HTML+PDF 初筛报告）| ✅ | `test_screen.py` 绿 |
| B5 `quantfox forecast`（前瞻分布 + `from_similar_valuation` 估值条件化）| ✅ | 见下方"杀手锏" |
| B6 选基方法论（`docs/quant-fund-selection-methodology.md` 10 条）| ✅ | 文档 |
| **B7-① fund-analyze 接入 forecast**（第5步 + 铁律 + 对话侧）| ✅ 本次 | `test_skill_file.py` 4 绿 |
| B3 舆情 | ✅ 已决策：**保持 WebSearch，重模块暂缓** | — |

**全量测试：`python -m pytest -q` → 78 passed（B7-① 后仍全绿）。**

**B5 杀手锏（务必理解，这是 v13 的核心价值）**：`quantfox forecast 002611` 实测——无条件持有60天正收益 65%/中位+2%，但 `from_similar_valuation`（从当前 77% 估值分位买入）只有 **44.76%/中位-0.97%**。即"用好看的无条件数字在山顶忽悠"被这个条件化直接戳破。**任何前瞻结论都要看中位、高位看 from_similar_valuation。**

## 3. 剩余工作（按优先级）
1. **B7-②（P1，主线）跨 skill 一致性**：forecast/看中位/估值闸门目前基本只在 fund-screener + fund-analyze。待同步：
   - `fund-watch`（持仓监控）加 forecast —— "这只从当前位置买/持有，未来赔率"。
   - `fund-compare` / `portfolio-manager` 补 forecast + 看中位 + 估值闸门。
   - 建议把"看中位不看均值 / 估值闸门 / 幸存者偏差"抽成**共享话术段**，7 个 skill 统一引用（避免各写各的、漏的漏）。grep 覆盖矩阵见 `skill-improvements.md` B7。
2. **A2（P1，唯一剩的代码项）自动 T+1 对账**：现为 SOP 指南。可选：`watch buy` 按 15:00 cutoff + 交易日历自动推确认日/净值，或对账用户报的 App 收益。
3. **文档瘦身（P3）**：`skill-improvements.md` 顶部 ✅ 段与底部 A/B 段**重复列了已完成项**，读着像还没做，建议收敛（把 A1-A3/B1-B6 的详情折叠进 ✅ 段）。

## 4. 关键 gotcha（踩过的坑，别再踩）
- **改前先确认真实文件状态**：本会话曾读到 stale 的旧 `screen.py/cli.py`（会话中途文件被更新），差点重造已存在的深筛。**动手前 `git log`/读现文件**。
- **akshare 限流**：eastmoney 板块资金流/行业板块接口本环境常 `RemoteDisconnected`；北向 `stock_hsgt_fund_flow_summary_em` 可用但粗。`fund_open_fund_*`（净值/榜单）稳定。舆情因此定为 WebSearch。
- **新命令/SOP 要重装插件生效**：`/plugin marketplace update quantfox → install → /reload-plugins`（用户操作，agent 跑不了）。
- **诚实红线（v13，不可退让）**：不承诺点数字；"高收益+高概率+短期"互斥必须当面点破；前瞻看中位、高位打折；保本优先在 `prompts/analysis_framework.md`（框架层，全 skill 隐式遵循）。
- **邮件**：`quantfox email send --attach <pdf>`，收件人默认 notify_to（读本地 `~/.quantfox` 配置，**邮箱等个人信息一律不写入仓库**），**绝不猜别的邮箱**；附 PDF 不附 HTML（QQ 不跑 JS）。

## 5. 待用户决策 / 未提交
- **package.json**：纯 Python 项目里的死 JS 脚手架（`${PROJECT_NAME}`→`money`），无人引用。建议删；等用户拍板。
- **uv.lock**：版本 0.1.2→0.1.5 与 pyproject 同步，合法，建议随下次提交带上。
- 以上两者**本次未提交**，仍在工作区。

## 6. 关键文件索引
- `docs/skill-improvements.md` —— 全量待办（A/B/B7，带优先级与✅状态）
- `docs/quant-fund-selection-methodology.md` —— B6 十条心智模型
- `docs/lesson.md` —— 操作教训（深筛≠看榜等）
- `docs/superpowers/specs/2026-07-10-deep-screening-sentiment-design.md` —— 深筛+舆情设计
- `quantfox/forecast.py` / `screen.py` / `screen_report.py` / `storage.py`(lots) —— 本轮核心代码
- `prompts/analysis_framework.md` —— v13 框架（保本优先在此）
